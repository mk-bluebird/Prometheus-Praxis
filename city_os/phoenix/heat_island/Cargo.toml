
[package]
name = "phoenix_heat_island"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Heat-island calibration and cooling priority logic for Phoenix hex grid."

[dependencies]
serde = { version = "1.0.203", features = ["derive"] }
serde_json = { version = "1.0.120" }

[dev-dependencies]
kani-verifier = "0.67"
