apiVersion: 1

providers:
  - name: "EcoNet Dashboards"
    orgId: 1
    folder: "EcoNet"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
