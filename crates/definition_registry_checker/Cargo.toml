# Filename: crates/definition_registry_checker/Cargo.toml
# Destination: crates/definition_registry_checker/Cargo.toml

[package]
name = "definition_registry_checker"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
