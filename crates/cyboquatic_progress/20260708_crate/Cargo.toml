# filename: eco_restoration_shard/crates/cyboquatic_progress/20260708_crate/Cargo.toml

[package]
name = "cyboquatic_drainagedecay_20260708"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
authors = ["mk-bluebird <bostrom18sd2ujv24ual9c9pshtxys6j8knh6xaead9ye7>"]
description = "Daily cyboquatic progress crate for 2026-07-08: drainage-decay frame with water-quality indicators and Lyapunov-safe eco-restoration scoring."

[lib]
name = "cyboquatic_drainagedecay_20260708"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
serde = { version = "1.0.203", features = ["derive"] }              # serde 1.0.203 is a valid release on crates.io [web:42]
serde_json = { version = "1.0.120" }                               # serde_json 1.0.120 is a valid release on crates.io [web:26]
rusqlite = { version = "0.31.0", features = ["bundled"] }          # rusqlite 0.31.0 is a valid release on crates.io [web:34][web:35]
kani-verifier = "0.67.0"                                           # kani-verifier 0.67.0 is a valid release on crates.io [web:25]

[dev-dependencies]
# Optional test helpers can be added here; Kani remains non-optional via the main dependency above.

[features]
# Keep default empty to avoid accidental feature gating of verification or ecosafety logic.
default = []
