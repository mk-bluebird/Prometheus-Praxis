{
  "name": "adaptive-corridors-mcp-server",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server exposing adaptive corridor diagnostics (Hflow CAP Phoenix, etc.) for Prometheus-Praxis.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.3.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "ts-node": "^10.9.2"
  }
}
